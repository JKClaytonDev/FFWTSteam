using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExplosionTakeControl : MonoBehaviour {
    /*
	// Use this for initialization
	void Start () {
        if (Time.realtimeSinceStartup < 5)
        transform.position = new Vector3(9000, 900, 900);
	}
	
	// Update is called once per frame
	void Update () {
        GameObject[] dummies = GameObject.FindGameObjectsWithTag("Explosion");
        if (dummies.Length > 1)
        {
            foreach (GameObject target in dummies)
            {
                if (target != gameObject)
                    Destroy(target);
            }
        }
    }
    */
}
